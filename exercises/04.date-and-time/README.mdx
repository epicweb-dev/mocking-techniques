# Date and time

1. Date and time (`vi.useFakeTimers()` + `vi.setSystemTime()`).
1. Timers (`vi.advanceTimers()` / `vi.advanceTimersToNextTimer()`). Handle `setTimeout` and `setInterval` in your tested code.
