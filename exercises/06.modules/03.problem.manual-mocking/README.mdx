# Manual mocking

- Using `vi.mock()` with an explicit `factory` function.
- [ ] What is the use case for manual mocking? When is it advantageous over the auto-mocking?
  - 1. Custom behavior for mocked exports. Vitest just replaces all with empty spies.
  - 1. Partial mocking. You can do `importActual()` and combine it with the mocked exports.
  - 1. A combination of the previous two.
